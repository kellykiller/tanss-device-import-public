<Window x:Class="TanssSystemCapture.Client.SapItemSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SAP-Artikel auswählen"
        Width="560"
        Height="380"
        MinWidth="480"
        MinHeight="330"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        FontFamily="Segoe UI"
        FontSize="14"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Mehrere SAP-Artikel gefunden"
                   FontSize="20"
                   FontWeight="SemiBold" />

        <TextBlock x:Name="InformationTextBlock"
                   Grid.Row="1"
                   Margin="0,10,0,14"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   TextWrapping="Wrap" />

        <ListBox x:Name="ItemCodesListBox"
                 Grid.Row="2"
                 Padding="6"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource PrimaryTextBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"
                 BorderThickness="1"
                 SelectionChanged="ItemCodesListBox_OnSelectionChanged"
                 MouseDoubleClick="ItemCodesListBox_OnMouseDoubleClick">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="10,7" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground"
                            Value="{DynamicResource PrimaryTextBrush}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"
                                    Value="{DynamicResource ControlHoverBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background"
                                    Value="{DynamicResource SelectionBrush}" />
                            <Setter Property="Foreground"
                                    Value="{DynamicResource PrimaryTextBrush}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="3"
                    Margin="0,18,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Content="Abbrechen"
                    Width="115"
                    Height="34"
                    Margin="0,0,10,0"
                    IsCancel="True" />
            <Button x:Name="ApplyButton"
                    Content="Übernehmen"
                    Width="125"
                    Height="34"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="ApplyButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
